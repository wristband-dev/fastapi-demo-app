{
  "name": "wristband-python-fastapi-accelerator",
  "version": "1.0.0",
  "description": "Full-stack application accelerator with FastAPI backend and Next.js frontend",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run database\" \"npm run frontend\"",
    "backend": "cd backend && python3 run.py",
    "database": "cd database && firebase emulators:start --only firestore --project wristband-fastapi-demo",
    "frontend": "cd frontend && npm run dev",
    "install-all": "npm install && cd frontend && npm install",
    "setup-python": "cd backend && python -m venv .venv && echo 'Python virtual environment created. Activate it with: source backend/.venv/bin/activate (Unix/Mac) or backend\\.venv\\Scripts\\activate (Windows)'",
    "install-python-deps": "cd backend && pip install -e .",
    "setup": "npm install && npm run setup-python && cd frontend && npm install",
    "install-firebase": "npm install -g firebase-tools && echo 'Firebase tools installed globally. You may need to login with: firebase login'",
    "postinstall": "node ./scripts/update-firebase-port.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
} 